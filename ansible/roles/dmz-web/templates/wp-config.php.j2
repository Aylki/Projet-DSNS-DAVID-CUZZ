<?php
/**
 * wp-config.php - Généré par Ansible
 * DMZ-WEB - itway.fr
 */

// ** Base de données **
define( 'DB_NAME',     '{{ wp_db_name }}' );
define( 'DB_USER',     '{{ wp_db_user }}' );
define( 'DB_PASSWORD', '{{ wp_db_password }}' );
define( 'DB_HOST',     'localhost' );
define( 'DB_CHARSET',  'utf8mb4' );
define( 'DB_COLLATE',  '' );

// ** Salts & Keys (générés aléatoirement) **
{{ wp_salts.content | default('') }}

// ** Table prefix **
$table_prefix = 'wp_';

// ** Debug (désactivé en prod) **
define( 'WP_DEBUG',         false );
define( 'WP_DEBUG_LOG',     false );
define( 'WP_DEBUG_DISPLAY', false );

// ** On est derrière un reverse proxy TLS **
// Nécessaire pour que WordPress génère des URLs https://
if ( isset( $_SERVER['HTTP_X_FORWARDED_PROTO'] ) && $_SERVER['HTTP_X_FORWARDED_PROTO'] === 'https' ) {
    $_SERVER['HTTPS'] = 'on';
}

define( 'WP_HOME',   'https://{{ domain_external }}' );
define( 'WP_SITEURL','https://{{ domain_external }}' );

// ** Sécurité : désactiver l'éditeur de fichiers dans l'admin **
define( 'DISALLOW_FILE_EDIT', true );

// ** Plugin OIDC - Keycloak (daggerhart-openid-connect-generic) **
// Ces constantes pré-configurent le plugin via wp-config
// (le plugin lit ces options depuis la DB, mais on les définit ici pour référence)
// Configurer via WP-Admin > Settings > OpenID Connect ou via WP-CLI :
//   wp option update openid_connect_generic_settings '{"..json..}'
// Voir : ansible/roles/dmz-web/tasks/configure_oidc.yml

// ** Limites **
define( 'WP_MEMORY_LIMIT',     '256M' );
define( 'WP_MAX_MEMORY_LIMIT', '512M' );

// ** Mises à jour automatiques désactivées (gérées par Ansible) **
define( 'AUTOMATIC_UPDATER_DISABLED', true );

/* C'est tout, ne rien ajouter après cette ligne. */
/** Chemin absolu vers le répertoire de WordPress. */
if ( ! defined( 'ABSPATH' ) ) {
    define( 'ABSPATH', __DIR__ . '/' );
}

/** Réglage des variables de WordPress et des fichiers inclus. */
require_once ABSPATH . 'wp-settings.php';
